# Filed

This draft became two issues on 2026-08-14:

- Issue #5 — Recognise a command by meaning, not just by how it sounded
- Issue #6 — Break up long spoken answers, not just lists the phone builds itself

The design it points at lives on in `doc/recognising-commands-by-meaning.md`.
Nothing here is current; this file exists only so the old link does not dangle,
and can be deleted.
